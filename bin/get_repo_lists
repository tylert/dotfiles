#!/usr/bin/env bash

github_user='tylert'

curl --silent --user ${github_user} \
  https://api.github.com/users/tylert/repos | grep ssh_url

curl --silent --user ${github_user} \
  https://api.github.com/orgs/{oarc,emrg-ca}/repos | grep ssh_url

#curl --silent --user ${github_user} \
#  https://api.github.com/orgs/{lightspeedretail,merchantos}/repos |\
#    grep ssh_url
